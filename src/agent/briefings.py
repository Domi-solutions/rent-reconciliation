"""Briefing and digest text generators."""


def generate_weekly_digest(conn, property_id):
    """Phase C placeholder."""
    return f"Weekly digest preview pending for property {property_id}."


def generate_caretaker_briefing(conn, property_id, caretaker_name):
    """Phase C placeholder."""
    return f"Morning briefing preview pending for {caretaker_name}."


def generate_owner_briefing(conn, property_id):
    """Phase C placeholder."""
    return f"Owner briefing preview pending for property {property_id}."


def generate_admin_checklist(conn, property_id):
    """Phase C placeholder."""
    return f"Admin checklist preview pending for property {property_id}."

